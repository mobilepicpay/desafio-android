import deps.AndroidTestDeps
import deps.Deps
import deps.TestDeps

plugins {
    id 'com.android.library'
    id 'kotlin-android'
    id 'kotlin-parcelize'
}

dependencies {
    implementation(
            Deps.kotlin,
            Deps.android,
            Deps.circleImageView,
            Deps.koin,
            Deps.retrofit,
            Deps.timber
    )

    testImplementation(
            TestDeps.junit,
            TestDeps.truth,
            TestDeps.mockk,
            TestDeps.coroutines
    )

    androidTestImplementation(
            AndroidTestDeps.runner,
            AndroidTestDeps.espresso
    )
}